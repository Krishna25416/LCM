#include<stdio.h>
void main()
{
   int a,b;
   printf("sum of two numbers");
   scanf("%d %d",&a,&b);
   int big = a>b? a:b;
   int step=big;
   while(1)
   {
      if (big %a == 0 && big%b==0)
        break;
    big=big+step;

   }
   printf("LCM of %d", big);
}




